#pragma once
#include <iostream>
#include <vector>

class block
{
public:
	block(int index, long timestamp, std::string prevHash, std::string Hash, std::vector<std::string> numbers, int nonce);

private:

	
};
